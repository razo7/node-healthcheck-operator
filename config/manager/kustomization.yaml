apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- manager.yaml

components:
- ../patches/affinity

generatorOptions:
  disableNameSuffixHash: true

#configMapGenerator:
#- files:
#  - controller_manager_config.yaml
#  name: manager-config

images:
- name: controller
  newName: quay.io/oraz/node-healthcheck-operator
  newTag: v0.6.1-testing-cyclic-remediation-5
